[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "muscles-sse"
version = "1.0.0"
description = "SSE adapter for Muscles streaming events"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = ["muscles>=1.0.0rc1,<2.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
